# Test config: MetabolismConfig v1 with user customizations
# Expected behavior: Should preserve custom values, only update version number
configVersion: 1
enabled: true
hunger:
  enabled: true
  baseDepletionRateSeconds: 720.0  # User customized (not 480)
  activityMultipliers:
    idle: 1.0
    walking: 1.5  # User customized multiplier
    sprinting: 2.0
    swimming: 1.8
    combat: 2.5
thirst:
  enabled: true
  baseDepletionRateSeconds: 540.0  # User customized (not 360)
  activityMultipliers:
    idle: 1.0
    walking: 1.2
    sprinting: 1.8
    swimming: 1.3
    combat: 2.2
energy:
  enabled: true
  baseDepletionRateSeconds: 900.0  # User customized (not 600)
  activityMultipliers:
    idle: 0.5  # User customized
    walking: 1.0
    sprinting: 2.0
    swimming: 1.6
    combat: 2.2
saveIntervalSeconds: 120  # User wants to save every 2 minutes
